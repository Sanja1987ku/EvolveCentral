using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace EvolveCentralConsole.DAL
{
    public static class Alert
    {
        //public static List<AlertItem> GetByService(entitiesEvolveCentralConsole ctx, int serviceid)
        //{
        //    var items = new List<AlertItem>();
        //    items = (from i in ctx.AlertItems where i.ServiceId  == serviceid select i).ToList();
        //    return items;
        //}

     
    }
}